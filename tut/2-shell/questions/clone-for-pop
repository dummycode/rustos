The pop method is only implemented for T that implement the clone trait. Without the clone trait, pop would be moving the value out of the array, which would be moving out from behind a mutable reference.
